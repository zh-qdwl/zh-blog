---
import BaseLayout from '../layouts/BaseLayout.astro';
import Comments from '../components/Comments.astro';
---

<BaseLayout title="留言" description="留言与交流" layout="plain">
  <article>
    <header class="article-header">
      <h1>留言</h1>
      <p class="lead">路过留个脚印，或者聊聊你在折腾什么。看到都会回。</p>
      <!-- 这三句是对访客个人数据的事实陈述，措辞必须站得住：
           「不公开显示 / 不进页面源码」是真的——Twikoo 与 Waline 的公开接口都只返回
           邮箱的 MD5（用于取头像），原文只有站长在后台可见，且评论数据全走客户端请求、
           不会被塞进静态 HTML。
           但不能写成「只用于回复通知」：邮箱还会被哈希用来显示头像，站长后台也看得到原文。
           为一句更漂亮的话把「只」加回去，就是拿别人的邮箱说了句不准的话。 -->
      <p class="notice">
        邮箱不会公开显示，也不会出现在页面源码里；只有我能看到，用于回复通知和显示头像。正文支持 Markdown。
      </p>
    </header>
    <!-- 留言区就是这一页的主体内容，懒加载没有意义，显式关掉 -->
    <Comments lazy={false} />
  </article>
</BaseLayout>

<style>
  .article-header {
    margin-bottom: 8px;
  }
  .article-header h1 {
    font-size: 2rem;
    margin: 0 0 8px;
  }
  .lead {
    margin: 0 0 10px;
    color: var(--text-soft);
  }
  /* 留言须知：比 lead 再轻一档，是提示而不是正文 */
  .notice {
    margin: 0;
    color: var(--text-soft);
    font-size: 0.85rem;
    line-height: 1.7;
  }
</style>
